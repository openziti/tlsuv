language: c

jobs:
  include:
    - stage: build all
      os: linux
      dist: bionic
      compiler: gcc

    - #stage: MacOS build
      os: osx
      osx_image: xcode11.2
      compiler: gcc

before_script:
  - env
  - mkdir build
  - cd build
  - cmake --config Debug ..

script:
  - cmake --build . --target uv_mbed
  - cmake --build . --target all_tests
  - ctest -VV --no-compress-output -C Debug -T Test
